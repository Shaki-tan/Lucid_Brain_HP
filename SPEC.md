# SPEC

Lucud Brain コーポレートサイトの仕様。

本書は仮確定である。判断の経緯と却下案は `Tasks/` 配下の作業文書に置いてあり、本書には結論だけを書く。
実装前に確認が要る事項は §10 に列挙した。

---

## 0. 本SPECのスコープ

- 本SPECが確定させるのは「サイトの構造と見た目の仕様」である。実装は別タスクとして切り出す。
- 掲載する文章・画像・ロゴといったコンテンツの中身は対象に含めない。実装時はプレースホルダを置き、内容は後から差し替える（§2.4）。

---

## 1. サイトの位置づけ

本リポジトリは Lucud Brain のコーポレートサイトである。

これまで単一プロダクト（Chronos）のLPだったが、次の2点により位置づけを変更した。

- プロダクト名が Chronos から Pawgress に変わった。
- サイトの主体が「プロダクトのLP」から「会社のHP」に変わり、プロダクトLPはその配下のサブページになった。

### 1.1 サイトの階層構造

サイトは3つの層で構成する。優先順位ではなく責務の分離である。
プロダクトやコンテンツが増えたときに、どの層へ足せばよいかが自明になることを狙う。

| 層 | 責務 | 対応するページ |
|---|---|---|
| 会社 | 我々が何者で、何を目指しているかを示す | トップページ（ミッション／事業／会社概要） |
| プロダクト | 個々のプロダクトを知ってもらい、試してもらう | `/products/<name>/` 配下 |
| 共通基盤 | 全ページが依存する規約・法的表記・問い合わせ導線 | ヘッダー／フッター／`/legal/` |

この層の分離が、以降の全ての構成判断の基準になる。
「会社の情報か、特定プロダクトの情報か、全ページ共通か」で置き場所を決める。

### 1.2 設計方針

大前提として、本サイトは平時は保守しない。
更新は「プロダクトを追加したとき」「価格や法的表記が変わったとき」「会社情報が変わったとき」に限られ、数か月触らない期間が常態である。

この前提から、以下が導かれる。

| 前提からの帰結 | 具体的にどうするか |
|---|---|
| 放置しても壊れないこと | 依存パッケージを持たない。バージョン更新や脆弱性対応が発生する構成を避ける |
| 久しぶりに触って分かること | 構造をSPECに書き切る。ディレクトリを見れば置き場所が分かる形にする |
| たまの修正で漏れが出ないこと | 同じ情報の出典を1箇所に限る。重複は物理的に排除する |
| 時間で劣化するものを置かない | 期限のあるトークン・署名に依存しない。外部APIのバージョンは明示的に固定する（§8.1） |
| Cloudflare 側の設定を増やさない | HTML / CSS / JS と、リポジトリ内の設定ファイルで済むものはそちらで解く |

最後の1行の理由は、保守性ではなく記録の残り方にある。
コードと設定ファイルは git に残って差分で追えるが、ダッシュボードで入れた設定は記憶にしか残らない。
数か月ぶりに触るとき、後者は「なぜそうなっているか」を復元できない。

したがって、同じ問題を解く手段が複数あるときは次の順で選ぶ。

1. HTML / CSS / JS で解く
2. リポジトリ内の設定ファイル（`_headers` / `_redirects` / `wrangler.jsonc`）
3. Cloudflare ダッシュボードの設定

これに加えて、次を守る。

- 意味のない装飾はしない。シンプルなページとする。
- 逆に、必要な記述は省略しない。
- 構成の簡潔さより、保守性とスケーラビリティを優先する。「ファイルが少ない」ことは目的ではなく、「増えたときに壊れない」ことを目的とする。
- ビルドツール・フレームワークは導入しない。素の HTML / CSS / JS のまま Cloudflare に載せる。npm 依存は「平時は保守しない」と正面から衝突するため（§7.3）。
- 継続的な更新が必要なコンテンツは、サイトに抱えず外部サービスに置く（§7.3）。

### 1.3 用語

| 語 | 意味 |
|---|---|
| 共通 | 2ページ以上で使うもの。`public/assets/` に置く |
| プロダクト固有 | 1プロダクトでしか使わないもの。`products/<name>/` 配下に置く |
| 正文 | 法的効力を持つ版。本サイトでは日本語（§2.2） |

---

## 2. 共通のページ要件

### 2.1 多言語

日本語と英語の2言語で提供し、英語ページも検索対象にする。

- 言語ごとに HTML を分け、URL も分ける。日本語は `/`、英語は `/en/` 配下に置く。
- 対応するページは同じ階層構造にする。`/products/pawgress/` に対して `/en/products/pawgress/`、`/legal/privacy` に対して `/en/legal/privacy`。
- 各ページに `<html lang="ja">` または `lang="en"` を置く。`canonical` は自己参照、`hreflang` は日英相互に張り、`x-default` は日本語を指す。
- i18n の辞書とエンジンは持たない。HTML が言語別なので不要になる。
- 言語による自動リダイレクトはしない。ブラウザ言語やアクセス元の国で勝手に飛ばさず、ヘッダーの言語切替リンクで移動させる。検索エンジン側が自動リダイレクトを推奨していないことと、EU の Geo-blocking 規則が「同意なき別言語版への自動リダイレクト」を禁じていることによる（§7.2）。
- 価格表示は日英とも円（JPY）表記のみとする。USD 等への換算は行わない。これは表示上の都合ではなく、海外法の適用を避けるための意図的な選択である（§7.2）。
- 販売地域は制限する（§7.2）。英語で見つかることと、どこに売るかは別に決める。

言語別ファイルにした理由は、1URL で JS 切替をする構成では英語が検索に出ないためである。
検索エンジンの登録単位は URL であり、1つの URL につき持てるレコードは1件しかない。
クローラは JS を実行した一度きりの結果を登録するが、言語切替はユーザーが押して起きるのでクローラは押さない。
検索結果に出るタイトルと説明文も1組しか持てず、英語のクエリに当たるには英語のタイトルが要るが、それは日本語のタイトルと同じ URL に同居できない。

副次的に、言語別ファイルにすると実装は単純になる。

| 不要になるもの | 理由 |
|---|---|
| i18n エンジンと辞書 | HTML が言語別なら要らない |
| 言語設定の `localStorage` 保持 | 言語切替が「別URLへのリンク」になる |
| `canonical` / `hreflang` の動的な扱い | 静的に正しく書ける |
| PDFリンクの言語追従ロジック | 各言語HTMLに直書きできる |

増えるのは HTML が7枚から14枚になることと、構造変更が2箇所になることである。
文言を書く量は変わらない。日英のズレは `tests/check-i18n.mjs` が見出しとセクションIDの一致で検出する（§7.5）。

### 2.2 `/legal/` の言語方針

legal 4種も言語別のファイルとする（`/legal/privacy` と `/en/legal/privacy`）。

- 正文は日本語とする。法的効力を持つのは日本語版であり、日本法に準拠する。
- 英語版は要約に留める。全訳はしない。訳文と原文に齟齬が生じたとき、全訳は不利に働くため。
- 英語版には「本ポリシーの正文は日本語であり、日本法に準拠する」旨を明示し、日本語版へのリンクを置く。
- 要約で省略した節には「日本語版に詳細がある」旨を書く。
- 準拠法と管轄裁判所を明記する。
- 本サービスが日本国内の居住者を対象とする旨を明記する（§7.2）。
- 購入フロー上で同意を求める事項だけは、英語でも本文と同等の内容とする（§8.3）。

根拠とリスクの整理は §7.2 にある。

### 2.3 非機能要件

| 項目 | 要件 |
|---|---|
| アクセシビリティ | `AGENTS.md` 第3部に準拠。操作要素は標準要素、アクセシブル名は必須、状態は `aria-*` で公開する |
| レスポンシブ | スマートフォン幅（〜400px）で横スクロールが発生しないこと |
| モーション | `prefers-reduced-motion` に従う |
| 和文の改行 | `word-break: auto-phrase` と `line-break: strict` で文節の途中で折らない。見出しは `text-wrap: balance`、段落は `pretty`。未対応ブラウザでは既定の折り返しに戻るだけとする |
| 文章の幅 | `--width-prose`（720px）と `--width-note`（620px）の2つだけを使う。ブロックごとに数値を決めない。幅が揃っていないと段落ごとに改行位置の傾向が変わる |
| パフォーマンス | 外部からの読み込みをゼロにする。フォントはセルフホストし（§7.5）、JS フレームワークを読み込まない |
| SEO | 全ページに `title` / `description` / `canonical`（絶対URL・自己参照）を持たせる。`hreflang` は日英相互に張り、`x-default` は日本語を指す |
| OGP | 全ページに OGP 画像・`og:title` / `og:description` / `twitter:card` を持たせる |
| 構造化データ | 持たない（§7.4） |
| セキュリティヘッダ | `_headers` で CSP / `X-Content-Type-Options` / `Referrer-Policy` / `frame-ancestors` を付与する（§7.5） |
| レイアウト安定性 | 画像に `width` / `height` を指定する。JS で差し込む領域は CSS で高さを確保する（§5） |
| キャッシュ | HTML は毎回再検証させる。CSS・JS は 60秒。ファイル名にハッシュを付けないため、長く持たせると「新しいHTML + 古いCSS」が発生する（§7.5） |
| ブラウザ | モダンブラウザの最新2バージョン。カスタム要素（Custom Elements v1）を前提とする（§5） |

### 2.4 コンテンツのプレースホルダ方針

文章・画像・ロゴは後から差し替える。実装時は以下に従う。

- テキストは `【仮】` を先頭に付けた日本語ダミーを置く。英語ページには `[TBD]` を付けた英語ダミーを置く。
- legal 本文と同意文言は、構造が分かる程度の例文を置く。節構成（見出しの階層と並び）は本番と同じにし、本文だけをダミーにする。
- 画像は `public/assets/images/placeholder/` に、正しい縦横比の無地プレースホルダを置く。実寸と比率は本SPECで確定させる。差し替え時にレイアウトが崩れないようにするため。
- ロゴと OGP 画像は、当面は既存の猫モチーフ SVG を流用する。差し替えは別途指示を受ける。
- 差し替え対象は `git grep '【仮】'` と `git grep '\[TBD\]'`、`placeholder/` で機械的に洗い出せる状態を保つ。

---

## 3. サイト構成（URL設計）

```
/                                   トップページ（会社HP。マニフェストもこの中）
/products/pawgress/                 Pawgress LP
/products/pawgress/thanks           Pawgress 決済完了ページ
/legal/privacy                      プライバシーポリシー
/legal/refund                       返金ポリシー
/legal/tokushoho                    特定商取引法に基づく表記
/legal/terms                        利用規約
/404                                404ページ

/en/                                英語トップ
/en/products/pawgress/              英語 LP
/en/products/pawgress/thanks        英語 決済完了ページ
/en/legal/privacy                   英語（要約）
/en/legal/refund                    英語（要約）
/en/legal/tokushoho                 英語（要約）
/en/legal/terms                     英語（要約）
/en/404                             英語 404

/docs/common/*.pdf                  プロダクトを跨いで通用するドキュメント（§4.3）
/docs/pawgress/*.pdf                Pawgress 固有のドキュメント（§4.3）
/legal/consent/<version>            同意文言の版ごとのスナップショット（§8.4）
/api/*                              Functions（§8）
/robots.txt, /sitemap.xml           クローラ向け
```

- 日本語は `/`、英語は `/en/` 配下に置き、対応するページは同じ階層構造にする（§2.1）。
- `/manifesto` は作らない。マニフェストはトップページ内のセクションとし、`/#manifesto` で直接参照する（§6.3）。
- プロダクトLPの階層は `/products/<name>/` とする。
- 決済完了ページはプロダクト配下に置く。プロダクトごとに文面・次のアクション・ダウンロードリンクが変わるため、共通の `/thanks` では早晩破綻する。遷移先は §8.2 のレジストリが `successPath` として保持し、Stripe の `success_url` に渡す。
- `sitemap.xml` には日英の全URLを載せる。PDF と `/legal/consent/*` も含める。
- 拡張子なしURLで配信する。`public/legal/privacy.html` は `/legal/privacy` で引ける（Workers Static Assets の既定の HTML ハンドリング。`.html` 付きURLは拡張子なしへ301される）。
- 現行の `/`（Chronos LP）は `/products/pawgress/` へ移る。既存のブックマークと被リンクが切れるため、`_redirects` に転送を置く。PDF の旧URL（`/docs/*`）も同様に確認する。

ドメインは未確定である。確定するまで `*.workers.dev` を前提に実装し、確定後に以下を併せて更新する。

- `wrangler.jsonc` の `name`
- `canonical` / `og:url` の絶対URL
- `sitemap.xml`
- Stripe の `success_url` / `cancel_url`（環境変数でベースURLを持たせ、コード変更が不要な形にする）

---

## 4. ディレクトリ構成

```
root
|--functions/                        # Cloudflare Workers 上で動くAPI
|  |--api/                           # HTTPエンドポイント。1ファイル1エンドポイント
|  |  |--checkout.js                 # POST /api/checkout
|  |  |--webhook.js                  # POST /api/webhook
|  |  |--download.js                 # GET  /api/download
|  |  |--download-free.js            # GET  /api/download-free
|  |--lib/                           # エンドポイント間で共有するロジック
|  |  |--products.js                 # プロダクト×プランのレジストリ（唯一の定義元）
|  |  |--stripe.js                   # Stripe API 呼び出しラッパ（APIバージョンの固定もここ）
|  |  |--entitlement.js              # DL可否の判定（買い切り／サブスクの差はここだけ）
|  |  |--regions.js                  # 販売しない地域の一覧と判定（§7.2）
|  |  |--r2.js                       # R2オブジェクトのストリーム配信
|  |  |--http.js                     # レスポンス生成・エラー整形
|  |  |--log.js                      # 構造化ログ（1行1JSON・§8.4）
|
|--public/                           # 公開されるファイルの全て
|  |--assets/                        # 2ページ以上で共有する資産
|  |  |--css/
|  |  |  |--tokens.css               # 色・タイポ・余白のCSS変数。単一の定義元
|  |  |  |--base.css                 # リセット + 要素既定
|  |  |  |--layout.css               # wrap / section / grid の骨格
|  |  |  |--components.css           # header / footer / button / card / dialog 等
|  |  |--js/
|  |  |  |--partials.js              # 共通ヘッダー／フッター。日英の文言もここに持つ（§5）
|  |  |  |--consent.js               # 購入同意ダイアログ。制御と日英の文言（§8.3）
|  |  |  |--main.js                  # 全ページ共通の初期化
|  |  |--fonts/                      # セルフホストするWebフォント（§7.5）
|  |  |--images/
|  |  |  |--logo/                    # ロゴ・ファビコン
|  |  |  |--og/                      # OGP画像
|  |  |  |--placeholder/             # 差し替え前のダミー（§2.4）
|  |
|  |--docs/                          # 配布用PDFは全てここ。第2階層で分ける（§4.3）
|  |  |--common/
|  |  |  |--gemini-api-key.ja.pdf
|  |  |  |--gemini-api-key.en.pdf
|  |  |--pawgress/
|  |  |  |--user-guide.ja.pdf
|  |  |  |--user-guide.en.pdf
|  |
|  |--products/
|  |  |--pawgress/
|  |  |  |--index.html               # Pawgress LP
|  |  |  |--thanks.html              # 決済完了
|  |  |  |--assets/                  # このプロダクト固有の資産。共通側と同じ並びにする
|  |  |  |  |--css/pawgress.css
|  |  |  |  |--js/
|  |  |  |  |--images/
|  |
|  |--legal/                         # 日本語（正文）
|  |  |--privacy.html
|  |  |--refund.html
|  |  |--tokushoho.html
|  |  |--consent/                    # 同意文言の版スナップショット。追加のみ・改変しない（§8.4）
|  |  |  |--2026-09-17.html          # 1ファイルに日英を併記する
|  |
|  |--en/                            # 英語版。日本語側と同じ階層構造にする
|  |  |--index.html
|  |  |--products/pawgress/
|  |  |  |--index.html
|  |  |  |--thanks.html
|  |  |--legal/
|  |  |  |--privacy.html
|  |  |  |--refund.html
|  |  |  |--tokushoho.html
|  |  |--404.html
|  |
|  |--index.html                     # トップページ（マニフェストを含む）
|  |--404.html
|  |--robots.txt
|  |--sitemap.xml
|  |--_headers                       # セキュリティヘッダ・キャッシュ制御
|  |--_redirects                     # 旧URLからの転送
|
|--setup/                            # セットアップ用CLIコマンド（未着手）
|  |--stripe/
|  |--cloudflare/
|
|--scripts/                          # 開発用スクリプト。配信されない（§7.5）
|  |--preview.mjs                    # public/ をキャッシュ無しで配信する確認用サーバー
|
|--tests/                            # 検査スクリプト（§7.5）。public/ の外に置く
|  |--check-placeholders.sh
|  |--check-links.sh
|  |--check-meta.sh
|  |--check-i18n.mjs
|  |--smoke.sh
|
|--Tasks/                            # 内部管理用。.gitignore 対象
|
|--.gitignore
|--AGENTS.md
|--README.md
|--SPEC.md
|--worker.js                         # Workers エントリポイント（明示ルーティング）
|--wrangler.jsonc
```

### 4.1 配置のルール

構成を維持するための判断基準。迷ったらここに立ち返る。

| 判断 | 置き場所 |
|---|---|
| 2ページ以上で使う | `public/assets/` 配下 |
| 1ページ／1プロダクトでしか使わない | そのページのディレクトリ配下（`products/<name>/...`） |
| 英語版のページ | `public/en/` 配下に、日本語側と同じ階層で置く |
| 英語版が使う CSS・画像 | 日本語側と同じものを参照する。複製しない |
| 全ページの見た目に現れる断片 | `public/assets/js/partials.js`（§5） |
| 単体で配布される文書（PDF等） | `public/docs/` 配下。プロダクト固有でも例外なくここに置く（§4.3） |
| APIエンドポイントそのもの | `functions/api/` に1ファイル1エンドポイント |
| 複数エンドポイントから呼ばれるロジック | `functions/lib/` |
| プロダクト固有の値（価格ID・R2キー・遷移先） | `functions/lib/products.js` のみ。ハンドラに直書きしない |

PDF だけは「1プロダクトでしか使わないものはプロダクト配下」の例外とする。
`assets/` は「ページが表示のために読み込む資産」だが、PDF は「単体で配布・共有される成果物」であり性質が違うこと、そして人に渡す URL は短いほうがよいことによる。

パスは全てルート相対（`/assets/...`）に統一する。
現行LPは `images/dashboard.png` のような相対パスで、サブディレクトリへ移した時点で全て壊れるため。

`assets/` の内部構造は、共通側もプロダクト側も同じ並びにする。

```
public/assets/{css,js,images,fonts}/                    共通
public/products/<name>/assets/{css,js,images,fonts}/    そのプロダクト固有
```

必要のない階層は作らなくてよいが、作るときの名前と順序は揃える。
「LP の画像はどこか」を毎回考えずに済ませるためである。

### 4.2 旧構成からの主な変更と理由

| 変更 | 理由 |
|---|---|
| `public/src/` を `public/assets/` へ | `src/` は「ビルドの入力」、`assets/` は「配信される資産」を指す語である。本サイトはビルドを持たず入力と出力の区別がない。しかも `src` は公開URLに出る（`/src/i18n.js`）ため、内部フォルダ名のつもりが閲覧者から見える |
| i18n エンジンと辞書を廃止 | 言語別に HTML を持つため不要になった（§2.1） |
| `public/en/` の新設 | 英語ページを検索対象にするため（§2.1） |
| `functions/lib/regions.js` の新設 | 販売しない地域を1箇所で定義するため（§7.2） |
| CSS を4ファイルに分割 | 1枚のままだとトップとLPのスタイルが混ざり、片方の修正が他方を壊す。トークンを分離すると配色変更が1ファイルで済む |
| `functions/lib/` の新設 | Stripe 呼び出し・R2配信・エラー整形が各ハンドラに重複しているため |
| `products.js` の新設 | プロダクト追加時に触るファイルを1つに閉じるため（§8.2） |
| `setup/Stripe/` を `setup/stripe/` へ | `AGENTS.md` 1.1 の「エコシステム標準ディレクトリは標準に従う」に合わせる |
| `_headers` / `_redirects` の追加 | セキュリティヘッダとURL移行を、コードではなくリポジトリ内の設定ファイルで扱うため（§1.2 の優先順位） |
| `assets/js/partials.js` の新設 | ヘッダー・フッターの重複を物理的に排除するため（§5） |
| `docs/` を第2階層で分ける | 配布用PDFを `public/docs/<区分>/` に一元管理するため（§4.3） |
| `assets/fonts/` の新設 | Webフォントをセルフホストし、外部読み込みをゼロにするため（§7.5） |
| `legal/consent/` の新設 | 同意文言の版を後から示せるようにするため（§8.4） |
| `tests/` の新設 | 検査スクリプトの置き場所（§7.5） |
| `manifesto.html` の削除 | マニフェストはトップページ内のセクションに統合（§6.3） |

### 4.3 `docs/` の配置

配布用のPDFは、プロダクト固有のものも含めて全て `public/docs/` 配下に置き、第2階層で区分する。

```
public/docs/
|--common/              プロダクトを跨いで通用する文書
|  |--gemini-api-key.ja.pdf
|  |--gemini-api-key.en.pdf
|--pawgress/            Pawgress 固有の文書
|  |--user-guide.ja.pdf
|  |--user-guide.en.pdf
```

URL は `/docs/common/gemini-api-key.ja.pdf` と `/docs/pawgress/user-guide.ja.pdf` になる。
会社案内やプレスキットは当面 `common/` に置き、会社の資料が増えたら `company/` を切る。

どちらの区分に置くかは「その文書は、そのプロダクトが無くなったときに一緒に捨てるか」で決める。
捨てるなら `docs/<product>/`、残るなら `docs/common/`。

| 文書の例 | 一緒に捨てるか | 置き場所 |
|---|---|---|
| Gemini / OpenAI の APIキー取得手順 | 残る（他プロダクトでも要る） | `docs/common/` |
| Pawgress の使い方 | 捨てる | `docs/pawgress/` |
| Pawgress へのAPIキー設定手順（アプリの画面説明） | 捨てる | `docs/pawgress/` |
| 会社案内・プレスキット | 残る | `docs/common/` |

現行の「APIキー取得ガイド」は、実は2本の文書が混ざっている。
「AIサービス側でキーを発行する手順」は共通、「発行したキーを Pawgress のどこに設定するか」は固有であり、移設時にこの線で分割する。

重複を防ぐため、次を守る。

1. 共通文書をプロダクト側にコピーしない。リンクする。同じPDFが2箇所にある状態を作らない。
2. 共通文書にプロダクト名を書かない。書いた瞬間に共通ではなくなる。書きたくなったら分割の合図である。
3. 迷ったらプロダクト側に置く。2つめのプロダクトが同じ文書を必要とした時点で `common/` へ昇格させる。
4. 昇格すると URL が変わる。外部に配った後では切れるため、配布前に判断を済ませる。

この配置の代償として、プロダクトを追加するとき `public/products/<name>/` と `public/docs/<name>/` の2箇所を作ることになる。
§4.1 の唯一の例外であり、文書の置き場所を探し回らずに済むことと配布URLが短いことを優先した結果である。
プロダクト追加時の手順（§8.2）にこれを明記して、作り忘れを防ぐ。

命名は `<名前>.<言語>.pdf` とする（`user-guide.ja.pdf` / `user-guide.en.pdf`）。
当面は日英で同一内容でも、ファイルは最初から分けておく。後から言語別に差し替えられるようにするためである。
各言語のページからは、それぞれの言語のPDFへリンクする。
短縮URL用の `_redirects` は張らない。最初から配布用のURLで配れば足りる。

---

## 5. 共通コンポーネントの扱い

素のHTML構成における最大の保守性リスクは、ヘッダーとフッターの重複である。
ページが N 枚あればヘッダーも N 箇所にコピーされ、フッターにリンクを1本足すだけで N ファイルの修正になる。
日英を合わせると14箇所になる。

このリスクは「平時は保守しない」前提のもとでむしろ大きい。
毎日触っていれば直し忘れに気づくが、数か月ぶりに触るときは気づかない。

そこで、マークアップを JS に持ち、カスタム要素で展開する。

```html
<head>
  <script src="/assets/js/partials.js"></script>
</head>
<body>
  <site-header></site-header>
  <main>…</main>
  <site-footer></site-footer>
  <noscript><!-- トップ・LP・legal への素のリンクを最小限だけ置く --></noscript>
</body>
```

```js
// assets/js/partials.js — マークアップはここに1箇所だけ持つ
const HEADER = `<header class="site-header"> … </header>`
const FOOTER = `<footer class="site-footer"> … © ${new Date().getFullYear()} … </footer>`

class SiteHeader extends HTMLElement {
  connectedCallback() { this.innerHTML = HEADER }
}
customElements.define('site-header', SiteHeader)
```

- `<script>` に `defer` を付けない。パーサが `<site-header>` に達する前に定義を済ませることで、`connectedCallback` が初回描画より先に走り、ちらつきが出ない。
- `fetch` しない。マークアップを JS ファイル自体に持つため、追加の往復が発生しない。
- コピーライトの年が自動で進む。平時保守しないサイトで、年始に古い年号が残るのを防ぐ。
- `site-header { display: block; min-height: … }` を CSS で確保し、レイアウトのずれを防ぐ（§2.3）。
- 文言は URL から言語を判定して出し分ける。`location.pathname.startsWith('/en/')` で日英を決め、`partials.js` 内に日英2組の文言を持つ。
- 言語切替リンクは、対応する別言語のURLを指す。`/products/pawgress/` にいるなら `/en/products/pawgress/` へ送る。自動リダイレクトはしない（§2.1）。
- ヘッダーが持つのは、左にロゴタイプ、右に言語切替だけとする。アイコンも節への導線も置かない（§6）。JS を実行しない環境向けのリンクは `<noscript>` が持つ。
- フッターが持つのは、左にコピーライト、右に legal への導線だけとする。販売対象地域の明記は置かない（§7.2）。
- 構成はコーポレート側とプロダクト側で分ける（§5.2）。範囲の判定は URL で行い、ページ側に属性を書かせない。

Worker の `HTMLRewriter` で配信時に注入する案は採らない。
静的アセットを Worker 経由に切り替える設定（`run_worker_first`）が必要になり、§1.2 に反するためである。
HTML と JS だけで同じ結果が得られるなら、そちらで解く。

### 5.1 受け入れるデメリット

JS を実行しないクローラにはヘッダー内リンクが見えない。本サイトでは実害が小さい。

- Googlebot は JS を実行する。
- 総ページ数が少なく、URL の発見は `sitemap.xml` が担う（§7.5）。
- SNS のカードは `<head>` の OGP を読むため、無関係である。

### 5.2 コーポレート側とプロダクト側で分けるもの

サイトは2つの範囲を持つ。範囲ごとに分かれるものと、分けないものを次のとおり定める。

| 範囲 | 該当ページ |
|---|---|
| コーポレート側 | `/`、`/legal/*`、`/404`（と英語版） |
| プロダクト側 | `/products/<slug>/*`（と英語版） |

| 対象 | 分けるか | 実装 |
|---|---|---|
| 配色 | 分ける | 定義元を範囲ごとに1つ持つ（下記） |
| ヘッダーのロゴタイプ | 分ける | コーポレート側は社名、プロダクト側はプロダクト名。それぞれ自分のトップへ送る |
| フッターの legal の並び | 分ける | プロダクト側は返金ポリシーを加える（購入の直前に読む・§8.3） |
| コピーライトの社名 | 分ける | プロダクト側だけ会社サイトへのリンクにする（§7.1） |
| ヘッダー・フッターの骨格（ヘッダーは左ロゴ・右言語切替、フッターは左コピーライト・右 legal） | 分けない | 範囲が変わっても並びは変えない |
| マークアップを持つファイル | 分けない | `assets/js/partials.js` に2組を隣に並べて置く |
| ヘッダー・フッターの見た目（CSS） | 分けない | `components.css` に1つだけ持つ。配色の変数が入れ替わることで面の色は変わる |

範囲の判定は URL で行う（`/products/<slug>/` に一致するか）。ページ側に属性を書かせない。
`<html>` や `<site-header>` に範囲を書かせると、ページを増やすたびに書き忘れが起きる。
URL から決まる事実を、URL 以外の場所に二重に持たないためである。

プロダクトの表示名は `partials.js` の `PRODUCTS` に持つ。未登録の slug は無視し、名前のないヘッダーを出さない。
プロダクトを増やすときは、ここと `functions/lib/products.js`（§8.2）の2箇所に足す。

#### CSS の置き場所

ヘッダーとフッターの見た目は共通の `components.css` に1つだけ持つ。
ページごとに分かれるのは「そのページの中身のスタイル」と配色であり、ヘッダーの見た目ではない。

| 対象 | 置き場所 | ページごとに変わるか |
|---|---|---|
| ヘッダー・フッターの見た目 | `assets/css/components.css` | 変わらない |
| 余白・書体の定義、コーポレート側の配色 | `assets/css/tokens.css` | 変わらない |
| トップページのセクション | `assets/css/layout.css` + 各ページ | — |
| Pawgress 固有のセクションと、Pawgress の配色 | `products/pawgress/assets/css/pawgress.css` | Pawgress だけ |

配色はコーポレート側とプロダクト側で分ける。プロダクトの見た目はプロダクトのものであり、
会社のコーポレートカラーに従う理由がない。プロダクトが増えれば、それぞれが自分の配色を持つ。

| 範囲 | 配色 | 定義元 |
|---|---|---|
| トップページ・legal・404 | 白基調 | `assets/css/tokens.css` |
| Pawgress のページ（LP・決済完了の日英4枚） | 黒基調 | `products/pawgress/assets/css/pawgress.css` |

変数名は両者で同じものを使う。プロダクト側は同名の変数を `:root` で上書きする。
こうすると共通の `base` / `layout` / `components` は、どちらの配色で描かれているかを意識しなくてよい。
プロダクトのCSSは共通CSSより後に読み込む（同じ詳細度のため、後に読むほうが勝つ）。
生の色を書くのは配色を定義するファイルだけとする原則は変えない。定義元が範囲ごとに1つあるだけである。

利用者のOS設定（`prefers-color-scheme`）では切り替えない。どちらの面で見せるかは範囲ごとに決まっているためである。
ブラウザUIの色は CSS 変数を参照できないため、白い側は `site.webmanifest` の `theme_color`、
Pawgress 側は各ページの `<meta name="theme-color">` が生の値を持つ。配色を変えるときはここも合わせる。

#### 1ページだけ見た目を変えたい場合

範囲ではなく特定のページだけヘッダーの見た目を変えたい場合は、カスタム要素に属性を渡して差分を表現する。
LP のヘッダーの背景を他より透かしているのがこれにあたる（決済完了ページには付けない）。
こうすると差分も共通側に1箇所だけ残り、後から「なぜ違うのか」が分かる。

```html
<site-header variant="lp"></site-header>
```

```css
/* components.css */
.site-header[data-variant="lp"] { background: transparent; }
```

---

## 6. トップページ

いわゆる普通の会社のホームページを1枚に収めたもの。
日本語の文言は確定済み（§6.4）。英語の文言と画像はプレースホルダのままとする（§2.4）。

| # | セクション | レイアウト | 高さの目安 | 内容 |
|---|---|---|---|---|
| 1 | ABOUT US | 左寄せ1カラム。ステートメント（`h1`）+ 導入文2段落。`id="about"` を持つ | 約 80vh | 確定 |
| 2 | WHAT WE DO | 2カラムのカード4枚（〜768px で1カラムに折返し）。`id="business"` を持つ | 可変 | 確定 |
| 3 | PRODUCTS & ACHIEVEMENT | 要約 + LPへのリンク。スクリーンショットは載せない。`id="products"` を持つ | 可変 | Pawgress 1件 |
| 4 | CONTACT | 社名 + 所在地 + X ／ メール ／ note。`id="contact"` を持つ | 可変 | 所在地は【仮】 |
| 5 | フッター | 共通コンポーネント。legal 3リンク + 販売対象地域 + コピーライト | — | 確定 |

本文の節は4つだけとする。会社概要と実績は独立した節を持たない（§6.2 / §6.3）。
節ラベルは4節とも同じ見た目にする（`.eyebrow` を `h2` に付ける）。大小や罫の有無で節の格付けをしない。
`h1` はトップページで1つだけであり、社名ではなくステートメントが受け持つ。社名はヘッダーのロゴタイプと `<title>` にある。

### 6.1 レイアウト規約

- 全セクションは共通の `wrap`（最大幅 1080px、左右 padding 24px）に収める。
- 本文は全て左寄せとする。中央寄せは使わない（行頭が揃わず、多行では読みにくい）。
- セクション間の垂直余白はトークン（`--space-section`）で統一し、個別指定しない。
- カードのグリッドは 3カラム、〜1024px で2カラム、〜768px で1カラムに折り返す。WHAT WE DO は4項目のため 2カラム（`.grid.grid-2`）で組む。
- ステートメントの改行位置は原稿で決まっているため `<br>` で固定する。ウィンドウ幅に任せない。
- スクロール誘導は置かない。第一画面に収まる量しか置いておらず、スクロールできることは自明である。

### 6.2 会社概要・実績の置き場所

- **会社概要** — 社名と所在地を CONTACT 節（#4）に置く。代表者と設立は特商法表記（`/legal/tokushoho`）が持つため重複させない。法的実体が未確定のため所在地は `【仮】` のままとする（§10.1）。
- **連絡先** — X ／ メール ／ note も CONTACT 節に置く。フッターには置かない。フッターは legal への導線とコピーライトだけを持つ。
- **実績** — 独立した節を作らず、PRODUCTS & ACHIEVEMENT の節に足す。実績が薄い状態で空の節を出すと、かえって信頼を損なう。掲載できるものが揃った時点でこの節に追記する。

外部リンクのURL（X ／ メール ／ note）は、この構成では共通コンポーネントを離れ、トップページの日英2ファイルに入る。
公開前の差し替えは2箇所になる。`tests/check-placeholders.sh` が `example.com` を拾うため、漏れは検出できる。

### 6.3 マニフェスト

`/manifesto` は作らない。全文も載せない。ABOUT US（#1）に凝縮した2段落として置く。

- 第一画面に置くため、参照は `/` で足りる。アンカー（`/#about`）は外部から直接指したいときのために残す。
- ページが1枚減ることで、共通ヘッダー／フッターの適用先も減る。
- 会社の姿勢を読ませたい相手（採用・取引先）を、別ページへ送って離脱させない。
- 第一画面で読み切れる長さに収める。全文を第一画面に置くと、スクロールされる前に離脱する。

`Tasks/メモ.md` の原稿は、凝縮前の出典として残す。長文版を掲載する場所は現時点では持たない。

### 6.4 文言の確定状況

| 対象 | 状況 |
|---|---|
| 日本語のステートメント・導入文・WHAT WE DO の4項目・Pawgress の要約 | 確定 |
| `<title>` / `description` / OGP（日本語） | 確定 |
| 英語版の同じ箇所 | `[TBD]` を付けた暫定訳。日本語を正とする |
| CONTACT 節の所在地 | `【仮】`。法的実体の確定後に差し替える（§10.1） |
| 利用規約（`/legal/terms`）の本文 | `【仮】` / `[TBD]`。他の legal と同じく公開前に実文が必須 |
| ロゴ・OGP画像・外部リンクのURL | プレースホルダ（§2.4 / §10.1） |

---

## 7. サブページと横断要件

### 7.1 Pawgress LP（`/products/pawgress/`）

現在の `public/index.html` を移設する。
構成は、ヒーロー／できること／ここが、他と違う。／はじめる前に／料金／最終CTA とする。

移設時の作業は次のとおり。

- パスのルート相対化
- 共通ヘッダー／フッターへの差し替え（§5）
- LP固有CSSを `products/pawgress/assets/css/pawgress.css` に切り出し
- 英語版 `/en/products/pawgress/` の作成（§2.1）
- 購入ボタンを同意ダイアログ経由に変更（§8.3）
- Chronos から Pawgress へのリネーム（§9。時期は別途指示）

LP から会社側への導線は、フッターのコピーライトの社名（`© <年> Lucud Brain`）1本だけとする。
パンくずリストや運営表記のように、本文やヒーローに導線を足さない。
読み手が見に来たのは会社ではなくプロダクトであり、目立つ位置に会社への導線を置いても離脱にしかならない。
コーポレート側ではこの社名をリンクにしない。ヘッダーのロゴタイプが同じ名前で同じ先を指すため、
同一画面に「役割 + 名前」が同じリンクが2つ並ぶのを避ける（AGENTS.md 3.2）。
なお `<noscript>` のリンク一覧はトップページを含む。JS を切った環境で行き先が無くなるのを防ぐためである（§5）。

ヒーローには見出し・本文・CTA だけを置く。対応OSや価格形態の注記は置かない。
同じ内容は「できること」以降の節と料金の節にあり、第一画面で二重に読ませる必要がない。
スクリーンショットも置かない。トップページと同じく、画面写真で説明しない方針に揃える（§6 の表・3行目）。

「ここが、他と違う。」の節（`id="highlights"`）は、他のツールで代わりにならない点を3つ、見出しのついた項目として並べる。

1. データが手元にあり、かつ AI がそれを読むだけでなく操作できること
2. 今日の着手順を Pawgress が決めること
3. 日程の矛盾を検出し、影響範囲をまとめて直せること

「できること」の節が機能の一覧であるのに対し、ここは選ぶ理由を置く場所である。同じ機能を二度説明しない。
1段落に詰めない。読み手が拾いたい一点を探すことになるためである。
上の「できること」がカードの並びなので、この節は1枚の面に項目を積んで、見た目でも役割の違いが分かるようにする。

「はじめる前に」の節（`id="setup"`）は料金の**前**に置く。
AIチャットが利用者自身のAPIキーで動くことは、買い手にとって前提条件であり費用でもある。
価格と購入ボタンを見たあとに知らせる順序にしない。

この節のカードは「使いはじめる前に知っておくこと」を1枚1問で並べる。粒度を揃えるための制約である。

1. AIに必要なもの（自分のAPIキー・対応サービス・キーの保存先）
2. AIにかかる費用
3. 無料版からの乗り換え（アンインストール不要・データはそのまま）

節の見出しは、中身のいずれか1つではなく3つ全体を指すものにする。
AI の話だけを指す見出しを付けると、乗り換えと手順書がその見出しの下に居ることの説明がつかなくなる。
配布資料（PDF）へのリンクは、カードではなくこの節の末尾のリストが持つ。手順書は読み物であって、
はじめる前に決めることではないため、同じ粒度に並べない。

### 7.2 海外販売のリスクと、本サイトの構え

以下は法律の専門家による助言ではなく、判断材料の整理である。金額が動く前に専門家に当てること。
また、内容は2026年9月時点のものであり、税務は改正が多い。

legal を英語化しないこと自体は違法ではない。
特定商取引法は日本の法律で、日本語で表示すれば表示義務は満たす。
リスクは翻訳の有無ではなく、英語ページから海外の消費者に売ること自体にある。
英語で商品を提示し購入導線が動いている状態は、その国の消費者へサービスを提供していると評価されうる。

英語ページを検索対象にする（§2.1）以上、海外からの流入は実際に発生する。
どこまで売るかは、閾値（何件目から義務が生じるか）で線を引く。

閾値がゼロ、つまり1件目から義務が生じる地域は次のとおり。

| 地域 | 乗ってくるもの | 備考 |
|---|---|---|
| EU | VAT（デジタルサービス） | 購入者所在国の税率。閾値なし |
| EU | GDPR ／ 消費者権利指令 | 返品権は §8.3 で対処済み。GDPR は要約のプライバシーポリシーでは足りなくなりうる |
| UK | VAT ／ UK GDPR ／ 消費者法 | 英国外事業者の B2C デジタル供給は閾値なし |
| 韓国 | VAT（電子的役務） | 国外事業者の B2C は実質的に閾値なし |
| インド | GST（OIDAR） | 国外からのデジタル提供は実質的に閾値なし |
| メキシコ | VAT 16% | 国外提供者に閾値なし |

閾値があり、少額のうちは実務上の問題になりにくい地域は次のとおり。

| 地域 | 乗ってくるもの | おおよその閾値 |
|---|---|---|
| 米国 | 州ごとの sales tax | 多くの州で年 $100,000 または200取引 |
| 米国 | CCPA / CPRA | 年商 $25M 等。小規模事業者は対象外 |
| カナダ | GST/HST | CAD 30,000 / 12か月 |
| オーストラリア | GST | AUD 75,000 / 年 |
| ニュージーランド | GST | NZD 60,000 / 年 |
| シンガポール | GST | **全世界** SGD 1M かつ現地 SGD 100,000 |
| ノルウェー | VAT | NOK 50,000 |
| スイス | VAT | **全世界売上** CHF 100,000 |

イラン・北朝鮮・シリア・キューバ・ロシア等への提供は制裁と輸出規制により禁止されるが、Stripe が決済段階で既にブロックしているため追加対応は不要である。

この2つの表は調べた時点のスナップショットであり、固定して運用できない。次の3点に注意する。

1. **網羅ではない。** メキシコは当初の整理から漏れていた。同様に漏れている地域がありうる。
2. **「閾値あり」に見えても、非居住者には適用されないことがある。** 登録閾値を自国内の事業者にだけ適用する制度は珍しくない。上の表は「安全な地域の一覧」ではなく、確認すべき順序として扱う。
3. **全世界売上が基準の閾値は、事業が伸びれば勝手に超える。** スイスとシンガポールは現地売上ではなく事業全体の売上が基準であり、日本国内の売上だけで超えうる。「その地域で売れていないから安全」という読み方ができない。

加えて、この領域は毎年変わる。2026年1月からもモーリシャスとアゼルバイジャンが国外事業者への登録義務を追加している。
したがって次のタイミングで一覧を見直し、`functions/lib/regions.js` に反映する。

| 見直しのトリガー | やること |
|---|---|
| 年1回（§7.5 のチェックリスト10番と同時） | 閾値ゼロ地域の一覧を更新する |
| 海外からの購入が実際に発生したとき | その国の扱いを個別に確認し、累積件数を記録する |
| 全世界売上が CHF 100,000（約1,800万円）に近づいたとき | 全世界売上基準の地域が一斉に対象化する |
| 英語圏への発信を強めるとき | 発信先の国が一覧に無いかを先に確認する |

#### 本サイトの構え

閾値がゼロの4地域（EU・UK・韓国・インド）からの購入を弾き、それ以外には売る。
そのうえで、以下でリスクを抑える。

1. EU・UK・韓国・インドを Worker 側で弾く（§8.1）。`request.cf.country` を見て `/api/checkout` を 451 で返し、LP 側でも購入ボタンを無効化する。
2. 価格は JPY 表記のみを維持する。「その国に向けた提供か」を判断する際、表示通貨は材料になる。JPY のみ・日本の事業者・日本語の特商法という組み合わせは、日本国内向けの事業であるというシグナルとして働く。
3. 「本サービスは日本国内の居住者を対象としています」と日英とも明記する。legal 4種と、同意ダイアログのチェック項目（§8.3）の2箇所に置く。

   フッターには置かない。全ページの最下部に出す文ではなく、購入の直前と規約本文で読ませるべき内容である。
   同意ダイアログでは購入者本人がチェックするため、「見えるところに書いてあった」より強い記録が残る（§8.3）。
4. legal の正文は日本語とし、英語は要約に留める（§2.2）。
5. 購入前に、返金不可への同意を明示的に取得し、記録する（§8.3）。
6. 準拠法と管轄裁判所を legal に明記する（日本法・特定の地方裁判所）。

#### 明記で消せるもの、消せないもの

3 の「明記」は万能ではない。対象によって効き方が違う。

| 対象 | 明記で効くか | 理由 |
|---|---|---|
| EU 消費者法 | 効く | 適用は「その国に活動を向けているか」で判断され、EU の判例はサイトがアクセス可能なだけでは足りないとする |
| GDPR | 効く | 3条2項も「EU のデータ主体へ提供しているか」を要件とし、意図が判断材料になる |
| EU VAT | 効かない | 税務当局への義務であり、実際に売った事実で発生する |
| 準拠法・管轄の指定 | 部分的 | 適用対象になった場合、消費者の常居所地国の強行規定は契約で排除できない |

VAT だけは明記で消せない。だから「弾く」が別途必要になる。
逆に言えば、弾いたうえで明記すれば、消費者法・GDPR・VAT の3つとも筋が通る。

明記は言行一致が条件である。書いておきながらその地域に売っていれば、記載は効かない。
本サイトでは4地域を弾く動作が伴うため、記載と行動が一致する。

**地域判定の限界。** IP ベースの判定には次の限界があり、これを受け入れる。

- VPN で回避できる。
- 旅行中の日本人が海外から買えなくなる（誤検知）。
- IP の国は居住国ともカード発行国とも一致せず、厳密な税務判定とは合わない。

完全な遮断ではなく、「その地域に向けて提供していない」という意思表示として機能させる。
JPY 表記のみと同じ性質の防御である。

### 7.3 ブログ

サイト内にブログを持たない。note 等の外部サービスに書き、フッターからリンクする。

- 本サイトは平時は保守しないため、継続的な更新を要するコンテンツを抱えるべきでない。
- 日英2言語のブログを手書きHTMLで運用するのは成立しない（1記事あたり日英2ファイルに加え、一覧・タグ・RSS の更新が要る）。
- SSG（Astro / Eleventy）を入れれば解決するが、npm 依存は「放置しても壊れないこと」と衝突する。記事を書かない数か月の間も、依存パッケージの脆弱性通知とビルド互換性の面倒は発生し続ける。
- 外部サービスなら、執筆・配信・RSS・購読者管理が全て向こうの責任になる。

Cloudflare の制約はこの判断の理由ではない。念のため確認したが、実質的に当たらない。

| 制約 | 値 |
|---|---|
| 静的アセット数／バージョン | 無料 20,000 / 有料 100,000 |
| 1ファイルのサイズ | 25 MiB |
| `_redirects` | 静的2,000 + 動的100 |
| `_headers` | 100ルール |

SSG がサイトを重くするわけでもない。ビルド時にHTMLを生成するだけで、配信されるのは静的HTMLである。
不採用の理由は速度ではなく、依存の持ち込みである。

リンク先は note を想定する。
ただし X アカウントやメールアドレスと同様、実装上はプレースホルダのまま進める。URL の確定は公開前でよい。

### 7.4 今後必要になりうるもの

「いま必要か」ではなく「今後必要になりそうか」で整理し、各項目に必要になるトリガーを置いた。

#### 初期リリースに含める

| 項目 | 何をするものか | 注意点 |
|---|---|---|
| `404.html` | 存在しないURLを開いたときに出るページ。今は素っ気ないエラー画面が出る | 日英2枚要る |
| `robots.txt` | サイト直下のテキスト1枚。クローラに「どこを見てよいか」「sitemap はどこか」を伝える | 誤記で全ページを除外する事故が起きうる。リリース前に実機確認する |
| `sitemap.xml` | サイト内の全URLを列挙したXML。検索エンジンが巡回先を漏れなく知るための地図 | 手書きで更新する。共通部品を JS で注入する以上（§5）、URL 発見の主経路になる |
| `_headers`（CSP 等） | 配信時に付けるHTTPヘッダの設定ファイル。CSP は読み込んでよい外部リソースをブラウザに宣言し、それ以外を実行させない | 書き方を誤ると本番でだけ壊れる。リリース前の実機確認が必須 |
| `site.webmanifest` / `apple-touch-icon` | スマホで「ホーム画面に追加」したときのアイコン名・色・画像を決める設定と画像 | ロゴ未確定のため後で差し替えが発生する |
| アナリティクス | 閲覧数と流入元を記録する。Cloudflare Web Analytics は Cookie を使わず、スクリプト1本を貼るだけ | 外部スクリプトが1本増え、§2.3 の「外部読み込みゼロ」の唯一の例外になる。`*.workers.dev` では自動注入が使えず手貼りになる |

構造化データ（JSON-LD）は採らない。
会社概要・製品名・価格が変わるたびに更新が要り、平時は保守しない前提では実体とズレたまま放置される可能性のほうが高いためである。ズレた構造化データは無いより悪い。
再検討するとしても、更新頻度が低い `Organization`（社名・ロゴ・SNS）だけに絞り、会社の法的実体が確定してからでよい。

#### 近い将来必要になる

| 項目 | トリガー | 注意点 |
|---|---|---|
| `/changelog`（Pawgress） | 有償版リリース時点 | 買い切り型は「まだ更新されているか」が購入判断に直結する。一方、更新が止まると逆効果になる |
| FAQ / サポートページ | 同じ問い合わせが3回来た時点 | 古い情報が残ると害になる。日英2枚ぶん増える |
| 問い合わせフォーム | メールへのスパムが実害になった時点 | Functions が増える。スパム対策は Cloudflare 設定が増え §1.2 と衝突する |
| HTML版の使い方ガイド | PDFが4〜5本を超えた時点、または検索流入を取りたくなった時点 | PDF と二重管理になりやすく、どちらかに寄せる判断が要る |
| お知らせ / `/news` | プレスリリースが必要になった時点 | 外部ブログと役割が重複する |
| 採用ページ | 人を採る意思が固まった時点 | 実体が決まる前に出すと不信を招く |

#### 不要と判断したもの

| 項目 | 判断 |
|---|---|
| USD 等の多通貨表示 | 不要。JPY のみが §7.2 の防御として機能している。再検討するとしても、海外売上が事業として無視できない規模になり、税務対応とセットで行う場合に限る |
| 会員機能・ログイン | 不要。ライセンスキー認証を行わない方針のため。ただしサブスクを導入する場合はここに半歩踏み込む必要が出る（§8.2）。その時点で再判断する |

### 7.5 横断要件（セキュリティ・検索・放置耐性）

個別のページではなく、サイト全体に掛かる要件。
「あとから足すのが高くつくもの」と「放置すると勝手に壊れるもの」を初期リリースに含める。

#### セキュリティ

| 項目 | 要件 | 備考 |
|---|---|---|
| CSP | `_headers` で `default-src 'self'`。インラインスクリプトを書かない構成にし、`unsafe-inline` を使わない | 許可先はアナリティクス1つで済む見込み。フォントをセルフホストし構造化データも持たないため |
| 基本ヘッダ | `X-Content-Type-Options: nosniff` / `Referrer-Policy: strict-origin-when-cross-origin` / `frame-ancestors 'none'` / `Permissions-Policy` は最小化 | `_headers` に数行 |
| Webhook 署名検証 | Stripe 署名とタイムスタンプ許容（300秒）を検証する | 実装済み（`functions/api/webhook.js`） |
| シークレット | `STRIPE_*` は Wrangler の secret として持ち、リポジトリに入れない | `.dev.vars` は `.gitignore` 済み |
| `/api/checkout` の濫用対策 | Origin を検証し、自サイト以外からの呼び出しを拒否する | Cloudflare の Rate Limiting は設定が増えるため採らない（§1.2） |
| R2 の保護 | バケットは非公開。配信は Worker 経由のみ | 満たしている |
| Webフォント | セルフホストする（`assets/fonts/`）。外部 CDN から読み込まない | 外部読み込みがゼロになり、プライバシーポリシーに書く第三者提供先が1つ減る |
| 受け入れるリスク | `session_id` を知る第三者はダウンロードできる | 独自トークン・期限・認証を持たない方針（§8.1）とのトレードオフ。設計上の受容であり欠陥ではない。拡散が実害になった時点で再検討する |

フォントのセルフホストについて補足する。
「Google Fonts の埋め込みが個人データ送信にあたる」という論点は、フォントファイルの中身ではなく通信そのものを指す。
外部 CDN から読むと閲覧者のブラウザが Google のサーバへ直接リクエストを出し、その際に IP アドレスが渡る。
ドイツの地裁がこれを同意なき個人データの第三国移転と判断した事例がある（2022年・賠償100ユーロ）。
判決の射程は狭く日本の事業者にそのまま及ぶものではないが、セルフホストのコストがほぼゼロであり、プライバシーポリシーの記載を減らせる利点のほうが大きいため採用する。

#### キャッシュ

ビルドを持たないため、CSS・JS のファイル名にハッシュを付けられない（§1.2）。
この構成では、資産を長くキャッシュさせると「新しいHTML + 古いCSS」の組み合わせが生まれ、
デプロイ直後の閲覧者にだけレイアウトが崩れた画面が出る。再現しづらく、気づきにくい壊れ方である。

| 対象 | Cache-Control | 理由 |
|---|---|---|
| 既定（`/*`。HTML を含む） | `public, no-cache` | 毎回再検証させる。変わっていなければ 304 で済む。拡張子なしURLで配信するため、パスの形で HTML だけを選び分けると漏れる（§3） |
| `/assets/*`、`/products/*/assets/*` | `public, max-age=60, must-revalidate` | 上記の窓を60秒に抑える。この規模では再訪時の速度への影響は無視できる |
| `/docs/*` | `public, max-age=600, must-revalidate` | 人に渡すURLであり、差し替えが起きうるので長く持たせない |

既定を `no-cache` にしてあるのは、上書きし損ねたパスが「古いものが残る」側ではなく
「毎回確認する」側に転ぶようにするためである。

手元での確認は `scripts/preview.mjs` を使う。`no-store` を返すため、キャッシュが混ざらない。
`*.workers.dev` はデプロイするまで古いままである。この2つを混同すると、直したものが出ていないように見える。

#### 検索最適化

| 項目 | 要件 |
|---|---|
| `canonical` | 全ページに絶対URLで自己参照。ドメイン確定まで暫定値 |
| `hreflang` | 日英を相互に張り、`x-default` は日本語を指す |
| 言語の自動リダイレクト | しない。切替はリンクで行う（§2.1・§7.2） |
| `sitemap.xml` | 日英の全URLを列挙する。`lastmod` は手で更新する |
| 構造化データ | 持たない（§7.4） |
| OGP | 1200×630。ページごとに `title` と `description` を分ける |
| Core Web Vitals | 画像に `width` / `height`、フォントは `font-display: swap`。JS で差し込む領域は CSS で高さを確保する。第一画面に画像を置かないため、`preload` する画像は持たない |
| 404 | ソフト404 にせず、実際に 404 ステータスを返すことを確認する |

#### 放置耐性

| 項目 | 要件 | 理由 |
|---|---|---|
| Stripe API バージョンの固定 | `Stripe-Version` ヘッダで明示的に固定する（`functions/lib/stripe.js`） | 指定しないとアカウント既定のバージョンに追従し、ある日突然壊れる |
| `compatibility_date` の固定 | `wrangler.jsonc` で固定する（現在 `2026-09-11`） | Workers 側の挙動変化に巻き込まれない。満たしている |
| Workers Logs | `wrangler.jsonc` に `"observability": { "enabled": true }` を置く | 障害時に手掛かりが残る。1行で済み、ダッシュボード操作が不要（§8.4） |
| リリース前チェックリスト | 手動で確認する項目を列挙する（下記） | ビルドを持たない構成での第1段階 |
| 再構築手順 | リポジトリだけでは復旧できないものを列挙する（下記） | 事故ってから書くのでは遅い |
| コスト | 放置しても増えるのは R2 のストレージ料金のみであることを確認する | 無料枠は Workers のリクエストと Workers Logs 20万件/日 |

#### リリース前チェックリスト

1. 主要ページが日英とも表示され、言語切替リンクが対応ページを指している
2. 共通ヘッダー／フッターが全ページで同じものになっている（§5）
3. 購入から同意ダイアログ、Stripe、thanks、ダウンロードまでが通る（テストモード）
4. 同意の版と時刻が Stripe の該当決済のメタデータに入っている（§8.4）
5. 閾値ゼロの4地域からの購入が弾かれる（§7.2）
6. CSP を入れた状態でコンソールにエラーが出ない
7. 存在しないURLが 404 を返す
8. `hreflang` の相互参照が全ページで揃っている
9. 外部リンク（ブログ・X）が生きている
10. legal 4種の記載が現行の法令・事業実態と合っている（年1回）
11. 閾値ゼロ地域の一覧が最新で、`functions/lib/regions.js` に反映されている（年1回・§7.2）

#### 検査スクリプトと CI

「ビルドを持たない」ことと「CI を持たない」ことは同じではない。
CI が壊れてもサイトは壊れないため、§1.2 の「放置しても壊れないこと」とは衝突しない。
むしろ GitHub Actions のワークフローはリポジトリ内の設定ファイルであり、§1.2 の優先順位にも合う。

依存をリポジトリに持ち込まずにできることは次のとおり。

| できること | 方法 |
|---|---|
| `【仮】` `[TBD]` の残存検出 | `grep` |
| 内部リンク・PDF の存在確認 | シェルスクリプト |
| 必須メタ（`title` / `description` / `canonical` / OGP）の欠落検出 | `grep` |
| 日英ページの構造が一致するか（見出し数・セクションID） | Node スクリプト（ランナーに同梱。`package.json` は作らない） |
| `hreflang` の相互参照が壊れていないか | シェルスクリプト |
| デプロイ後のスモーク（主要URLが 200 / 404 を返すか、CSP ヘッダが付いているか） | `curl` |

第1段階は手動チェックリスト、第2段階として公開後に CI を入れる。
トリガーはページ数が10枚を超えた時点、または手動チェックで漏れが1回出た時点とする。

採る場合の条件は3つ。`package.json` を作らないこと、アクションのバージョンを固定すること、落ちてもデプロイは止めないこと（通知のみ）。

検査スクリプトはリポジトリ直下の `tests/` に置く（`AGENTS.md` 1.1 のエコシステム標準）。

```
tests/
|--check-placeholders.sh   # 【仮】[TBD] の残存検出
|--check-links.sh          # 内部リンク・PDF の存在確認
|--check-meta.sh           # title / description / canonical / OGP の欠落検出
|--check-i18n.mjs          # 日英ページの構造一致と hreflang の相互参照
|--smoke.sh                # デプロイ後、curl で 200 / 404 / CSPヘッダを確認
```

`public/` の下には絶対に置かない。置いたものはそのまま公開されるためである。
ローカルから `bash tests/check-links.sh` で単体実行できる形にし、CI 専用にしない。
手動チェックリストと同じものを、人が回すか CI が回すかの違いにする。
CI（`.github/workflows/*.yml`）はこれらを呼ぶだけにする。

**再構築手順に必要な情報。** リポジトリには無く、事故ったときに必要になるもの。

- R2 バケット名と、中に置く exe のキー（§8.2 のレジストリと一致させる）
- Stripe の商品・価格ID、Webhook の宛先とイベント種別、署名シークレット
- Wrangler の secret 一覧（名前だけ。値は持たない）
- ドメインと DNS の設定先
- Cloudflare / Stripe アカウントの管理者と、登録している支払い方法
- Stripe を解約・乗り換えする場合は、事前に決済履歴を CSV でエクスポートする（§8.4）

---

## 8. バックエンド構成

### 8.1 全体の設計

- 構成は GitHub から Cloudflare Workers（静的アセット + Functions）、Stripe、Cloudflare R2。
- 無料版・有料版とも、exe実体は R2 で管理する。公開ディレクトリに置くと、リンクの有無に関わらず直接URLで取得できてしまうため。
- 有料版は Stripe Checkout（カード決済のみ）を使う。決済確認は Checkout Session ID をそのまま認可情報として扱い、リクエストの都度 Stripe API で `payment_status === 'paid'` を照会する。独自のトークン発行と署名管理は行わない。この方針は §1.2 の「時間で劣化するものを置かない」と整合している。
- ダウンロードURLに独自の有効期限は設けない。
- Stripe Webhook はダウンロード可否判定の必須経路にしない。用途は「Stripe 側で起きたことに気づくための通知」であり、記録の保存先ではない（§8.4）。
- ルーティングは `worker.js` で明示的に行う（Pages Functions の自動ルーティングに問題が生じたため）。ハンドラ実体は `functions/api/*.js` を再利用する。Worker が見るのは `/api/*` のみとし、静的アセットは Worker を経由せず配信する。
- R2 バケット名 `chronos-releases` とオブジェクトキーは据え置く。内部名であり外部に露出しない。現行の実キーは `latest/Chronos-Setup.exe`（有償）と `latest/Chronos-Free-Setup.exe`（無償）。
- Stripe API のバージョンを `Stripe-Version` ヘッダで固定する。`functions/lib/stripe.js` の1箇所で付与する。
- `/api/checkout` は Origin を検証する（§7.5）。

**販売地域の制限。** 閾値がゼロの4地域（EU・UK・韓国・インド）からの購入を弾く（§7.2）。

Stripe Checkout には請求先国の許可リストが無い。`allowed_countries` は配送先住所用のパラメータで、デジタル商品では使えない。
Radar のカスタムルールなら可能だが、有料アドオンとダッシュボード設定が必要になるため採らない（§1.2）。

したがって Worker 側で判定する。対象リストと理由は `functions/lib/regions.js` に置く。

```js
// functions/api/checkout.js の冒頭
const country = request.cf?.country            // Cloudflare が付与する接続元の国コード
if (isBlockedRegion(country)) {
  return json({ error: 'unavailable_in_region' }, 451)
}
```

LP 側でも同じ判定で購入ボタンを無効化し、決済直前ではなく手前で伝える。

**現時点で地域判定を入れるのは `/api/checkout` だけであり、`/api/download-free` は誰からでも叩ける。**
無料版には決済が絡まないため、§7.2 の税務上の論点（VAT・GST）は発生しない。
一方で、次の2点は税務とは別に検討が要る。専門家確認の対象とする（§10.2）。

- **制裁対象国への無償配布**。決済が無くてもソフトウェアの配布自体が輸出規制に触れるかどうか。
- **無償利用者に対する GDPR**。無償でも適用されうるが、本製品はローカル完結でこちらがユーザーデータを保持しないため、実務上の負荷は小さい。関係するのはダウンロード時のログに残る情報程度である（§8.4 のとおり保持は3〜7日）。

確認の結果しだいで、`/api/download-free` にも同じ判定を入れるか、現状のまま開けておくかを決める。
**いまは「決めていない」のではなく「確認待ち」の状態として扱う。**

### 8.2 プロダクトレジストリ

プロダクトが2つめに増えたときに API を増やさずに済ませるための仕組み。
現状は `checkout.js` と `download.js` にプロダクト固有の値が直接書かれており、プロダクトを足すたびにエンドポイントが倍増する。

さらに料金体系（買い切り／サブスク）という第2の軸が加わるため、レジストリは「プロダクト × プラン」の2次元で持つ。

```js
// functions/lib/products.js — 唯一の定義元
export const PRODUCTS = {
  pawgress: {
    displayName: 'Pawgress',
    successPath: '/products/pawgress/thanks',
    cancelPath:  '/products/pawgress/#price',
    plans: {
      free: {
        billing: 'free',
        r2Key: 'latest/Chronos-Free-Setup.exe',   // R2 のキーは据え置き（§8.1）
        downloadName: 'PawgressFreeSetup.exe',
      },
      paid: {
        billing: 'one_time',                      // 買い切り
        checkoutMode: 'payment',                  // Stripe Checkout の mode
        priceEnvKey: 'STRIPE_PRICE_PAWGRESS_PAID',
        r2Key: 'latest/Chronos-Setup.exe',
        downloadName: 'PawgressSetup.exe',
        consentSet: 'one_time',                   // 同意文言のセット（§8.3）
        consentVersion: '2026-09-17',
      },
      // サブスクを足す場合の形（現時点では定義しない）
      // pro: {
      //   billing: 'subscription',
      //   checkoutMode: 'subscription',
      //   priceEnvKey: 'STRIPE_PRICE_PAWGRESS_PRO',
      //   consentSet: 'subscription',            // 自動更新・解約条件を含む別セット
      //   hasPortal: true,                       // 解約導線に Stripe Customer Portal を使う
      // },
    },
  },
}
```

エンドポイントは `?product=pawgress&plan=paid` を受け、レジストリに無い組み合わせは 400 を返す。

プロダクト追加時に触るのは次の3つだけとする。プラン追加時は `plans` の1エントリと環境変数1つで済む。

1. `functions/lib/products.js` に1エントリ
2. `public/products/<name>/` を作る（LP・thanks・固有assets）と、`public/en/products/<name>/`
3. `public/docs/<name>/` を作る（配布用PDF。§4.3 の例外）

`worker.js` のルーティングもルートテーブル（メソッド・パス・ハンドラの配列）に置き換え、分岐の `if` が増え続けない形にする。

#### 買い切りとサブスクの差

差は4箇所に閉じる。サブスクを足すときも、この4箇所以外には手が入らない。

| 箇所 | 買い切り | サブスク | 吸収方法 |
|---|---|---|---|
| Checkout 作成 | `mode=payment` | `mode=subscription` | `plan.checkoutMode` をそのまま渡す |
| DL 可否の判定 | `session.payment_status === 'paid'` | session から subscription を引き、`status` が `active` / `trialing` か | `plan.billing` で分岐。`functions/lib/entitlement.js` の1関数に閉じる |
| 同意項目 | 4項目 | 追加が要る（自動更新の了承・解約方法の確認） | `plan.consentSet` で切り替え（§8.3） |
| 解約導線 | 不要 | Stripe Customer Portal（`/api/portal`）が必要 | `plan.hasPortal` で LP と thanks にボタンを出す |

器は用意するが、サブスクの認可設計は導入を決めた時点で別途起こす。理由は次のとおり。

- 買い切りなら `session_id` は「一度買った事実」を指すため永続的に使えるが、サブスクでは利用者が thanks の URL を自分で保管し続けることになり、失くすと再取得手段がない。
- 再取得の導線を作るには「メールでDLリンクを送る」か「Customer Portal 経由で本人確認する」のいずれかが要り、§7.4 で不要とした会員機能に半歩踏み込む。
- 特商法の定期購入規制により、最終確認画面（同意ダイアログ）に金額・支払時期・自動更新の有無・解約方法・解約期限の表示が要る。特商法表記ページの記載も増える。
- Webhook で `customer.subscription.deleted` と `invoice.payment_failed` の処理が要る。

### 8.3 購入フローと同意取得

§7.2 の EU 消費者権利指令への対処である。購入前に同意を取得し、記録する。

```
  [有償版を購入] ボタン
        |  押下
        v
  +-----------------------------+
  |  同意ダイアログ（モーダル）     |
  |                             |
  |  [ ] 同意項目1               |
  |  [ ] 同意項目2               |
  |  [ ] 同意項目3               |
  |  [ ] 同意項目4               |
  |                             |
  |        [ 決済に進む ]        |  <- 全チェックが入るまで無効
  +-----------------------------+
        |  押下
        v
  POST /api/checkout（同意情報を添えて送信）
        |
        v
  Stripe Checkout（カード決済）
        |
        v
  /products/pawgress/thanks -> ダウンロード
```

同意チェックは購入ボタンの手前には置かない。
ボタンを押してから同意ステップに入り、全項目にチェックが入って初めて「決済に進む」が有効になる。
こうすることで、料金セクションが煩雑にならない。

#### 同意項目

買い切り（`consentSet: 'one_time'`）は4項目とする。
実文は別途準備中のため、以下は構造を示す例文である（§2.4）。

| # | 主旨 | 例文 |
|---|---|---|
| 1 | 返金ポリシーへの同意 | 【仮】返金ポリシーの内容を確認し、同意します。 |
| 2 | 返金不可となる条件の承諾 | 【仮】ダウンロードを開始した時点で返金を受けられなくなることを承諾します。 |
| 3 | 特商法表記の確認 | 【仮】特定商取引法に基づく表記を確認しました。 |
| 4 | 対象地域の確認 | 【仮】本サービスが日本国内の居住者を対象としていることを確認しました。 |

項目2 が EU 消費者権利指令への対処の核心であり、これがないと返品権が残る。

項目4 は §7.2 の「明記」の一部である。
フッターや legal に書くだけでなく購入者本人がチェックすることで、「見えるところに書いてあった」ではなく「本人が確認した」という記録になり、その事実が §8.4 の仕組みで Stripe に残る。

- 同意項目はプラン単位で持つ。`plan.consentSet` で切り替える構造にしておき、サブスクを導入するときは `subscription` のセットを追加する。その内容は導入時に別途指示を受ける。
- 各項目から該当する legal ページへリンクする（別タブで開く）。
- 日英とも本文と同等の内容を表示する。§2.2 の「英語は要約」の唯一の例外である。ここを要約にすると返品権が残る話に直撃するため。
- 文言は `assets/js/consent.js` に日英2組を持つ。

**記録。** `POST /api/checkout` に同意の事実を渡し、Stripe 側に記録する。詳細は §8.4 にある。
同意文言を変更したら `products.js` の `consentVersion` を上げ、`public/legal/consent/<version>.html` にその版のスナップショットを追加する。

#### アクセシビリティ

`AGENTS.md` 3.2 に準拠する。

| 項目 | 要件 |
|---|---|
| ダイアログ | `role="dialog"` / `aria-modal="true"`。見出しを `aria-labelledby` で参照する |
| フォーカス | 開いたら内部にフォーカスを移し、フォーカストラップを掛ける。閉じたら呼び出し元ボタンへ戻す |
| キーボード | Esc で閉じる |
| チェックボックス | 標準の `<input type="checkbox">` を使う。`div` にハンドラを付けない |
| 無効状態 | 「決済に進む」の無効は `aria-disabled` で公開する。見た目のみで表現しない |

### 8.4 ログと記録

法的に意味を持つ記録は Stripe に置き、Workers Logs は障害調査専用とする。

現行の `webhook.js` はイベントを `console.log` しているだけで、その出力は Workers Logs に無料3日・有料7日しか残らない。
監査に耐える記録は Stripe 側にある。

#### 原則

| # | 原則 | 理由 |
|---|---|---|
| 1 | 法的に意味を持つ記録は Stripe に置く | 決済レコードと同一オブジェクトに紐づき、「この決済のときにこの同意があった」が1画面で示せる |
| 2 | Workers Logs に消えて困るものを入れない | 保持が3〜7日しかない |
| 3 | 自前のログに個人情報を書かない | 氏名・メール・IP は Stripe が持つ。二重に持つと管理対象が増えるだけ |
| 4 | 時刻はサーバ側で採る | クライアントの時計は改竄可能でタイムゾーンも不定。送られた値は参考値として別キーに入れる |
| 5 | 1行1JSON で出す | Workers Logs でフィールド検索できる。`functions/lib/log.js` に集約する |
| 6 | 版番号だけでなく文言そのものを残す | どの版に同意したかが分かっても、その版の文言が残っていなければ示せない |

#### 何を、どこに残すか

| イベント | 発生箇所 | 残す項目 | 残す先 | 保持 |
|---|---|---|---|---|
| 同意 | `/api/checkout` | `consent_version` / `consent_at`（サーバ時刻）/ `product` / `plan` | Stripe の metadata | 実質恒久 |
| 決済成立・返金・異議 | Stripe | 金額・国・支払手段・時刻 | Stripe（自動） | 実質恒久 |
| Checkout 作成の失敗 | `/api/checkout` | エラー種別・Stripe のステータス | Workers Logs | 3〜7日 |
| ダウンロード実行 | `/api/download` | `product` / `plan` / `session_id` の末尾数桁 / 結果 | Workers Logs | 3〜7日 |
| Webhook 受信 | `/api/webhook` | イベント種別・ID | Workers Logs | 3〜7日 |

Stripe へは、Session と支払い側（PaymentIntent / Subscription）の両方に同じ値を書く。

```js
params.set('metadata[consent_version]', plan.consentVersion)
params.set('metadata[consent_at]', consentAt)          // サーバ時刻の ISO8601
params.set('metadata[product]', productId)
params.set('metadata[plan]', planId)
// 買い切り: payment_intent_data[metadata][…] / サブスク: subscription_data[metadata][…] にも同じ値
```

Session の metadata だけでは、ダッシュボードの「支払い」詳細画面に出てこない。
支払い側にも書くことで、人が普通に辿り着ける画面のメタデータ欄に現れる。
`client_reference_id` に `pawgress:paid` を入れると一覧でも判別できる。
Stripe の metadata は1オブジェクト50キー、キー40文字・値500文字までで、本用途では余裕がある。

#### 記録はどこで見るか

この構成に、いわゆるログファイルは存在しない。サーバのディスクに `.log` が貯まる形ではない。
見る場所は3つあり、用途が違う。

| 見る場所 | 何が見えるか | 保持 |
|---|---|---|
| Stripe ダッシュボード → 支払い → 対象の決済 | 同意の版と時刻（メタデータ欄）、金額、購入者、返金・異議 | 実質恒久 |
| Stripe ダッシュボード → 開発者 → イベント / ログ | Webhook 配信履歴、API リクエスト履歴 | 30日程度 |
| Cloudflare ダッシュボード → Workers & Pages → 該当 Worker → Observability | `console.log` の出力（ダウンロード実行・エラー） | 無料3日 / 有料7日 |

ローカルからリアルタイムに見るなら `npx wrangler tail` を使う。
Workers Logs を確実に有効化するため、`wrangler.jsonc` に1行入れる。無料枠は20万件/日である。

```jsonc
"observability": { "enabled": true }
```

Logpush、R2 への書き出し、D1 は採らない。
有料機能・追加バインディング・スキーマ保守が発生し、§1.2 の「Cloudflare 側の設定を増やさない」「放置しても壊れない」の両方と衝突するためである。

Stripe Checkout の `consent_collection[terms_of_service]` も併用しない。
Stripe 側が取得した記録になる点は強いが、ダッシュボードでの規約URL設定が必要になり、利用者が同趣旨のチェックを2度することになる。自前ダイアログと metadata で要件は満たせる。

**同意文言そのものの保存。** `consent_version` が残っても、その版の文言が無ければ記録として弱い。
`public/legal/consent/<version>.html` に版ごとのスナップショットを置き、追加のみとして改変しない。
同意ダイアログからもこのURLへリンクする。ファイルが増えるのは文言を変えたときだけで、年1〜2回にとどまる。

#### 実際の場面でどう辿るか

記録の設計は「問い合わせが来たときに何分で答えられるか」で評価する。

返金を求められた場合は、Stripe ダッシュボードの支払いから該当の決済を開き、メタデータ欄で `consent_version` と `consent_at` を見る。
`/legal/consent/<version>` でその版の文言を確認し、返金するなら同じ画面の返金ボタンを押す。Stripe の1画面で完結する。

EU の購入者が撤回権を行使すると言ってきた場合も同じ手順で、購入前に「DL開始で返金不可」へ同意していた事実と、同意時刻が決済時刻より前であることを示す。
実際にダウンロードしたかは Workers Logs で見るが、3〜7日を過ぎていれば残っていない。その場合は Stripe 側の同意記録だけで対応する。同意の事実が本体であり、DL の有無は補強材料である。

購入したのにダウンロードできないと言われた場合は、thanks の URL か注文日時をもらい、Cloudflare の Observability で `session_id` の末尾数桁や時刻から絞る。
`/api/download` のログで、403 か R2 の 404 か Stripe 照会の失敗かを切り分ける。数日経ってログが消えていたら、同じ URL を叩いてもらって再現させる。

チャージバックの通知が来た場合、証拠として提出できるのは同意の版と時刻、そしてその版の文言である。
この2つが揃っていることが、同意を metadata に残す最大の理由である。

なお「先月何本売れたか」「どのページから購入に至ったか」はこの設計では答えられない。
Stripe の売上レポートとアナリティクス側の話であり、ログの役割ではない。

**保持と出口。** Stripe の記録はアカウントが有効な限り残り、帳簿の保存年限も満たす。
ただし Stripe を解約すると消えるため、解約や乗り換えの際は決済履歴を CSV でエクスポートする（§7.5）。

---

## 9. Chronos から Pawgress へのリネーム

実施時期は別途指示を受ける。本SPECでは影響範囲の記録に留める。

| ファイル | 出現数 | 備考 |
|---|---|---|
| `public/index.html` | 7 | LP本体 |
| `public/src/i18n.js` | 11 | 辞書。§2.1 により廃止するため、移設時に消える |
| `public/thanks.html` | 4 | |
| `public/legal/*.html` | 各2 | |
| `public/src/script.js` | 1 | |
| `functions/api/download.js` | 2 | R2キー |
| `functions/api/download-free.js` | 2 | R2キー |
| `wrangler.jsonc` | 2 | `name`, `bucket_name` |

- 表記は `Pawgress` とする。P のみ大文字で、以降は小文字。
- ファビコンとロゴの猫モチーフ（SVG）は、名称変更後も意味が通るため流用できる。
- R2 のバケット名とオブジェクトキーはリネーム対象外とする（§8.1）。

---

## 10. 未確定事項

### 10.1 本SPECの対象外（後から指示を受ける）

| 事項 | 備考 |
|---|---|
| ドメイン | 確定まで `*.workers.dev` 前提。確定後の更新箇所は §3 に記載済み |
| 会社の法的実体・正式名称・会社概要の内容 | 特商法表記と直結する。公開前に必須 |
| 事業内容の文言 | プレースホルダで実装を進める |
| ミッションとマニフェストの本文 | 同上。原稿は `Tasks/メモ.md` にあるが、メモ段階のため採用しない |
| 実績 | 揃うまでセクションを非表示にする（§6.2） |
| X アカウント・メールアドレス・ブログのURL | プレースホルダで実装を進める |
| legal 4種の本文（日英） | 例文で実装を進める。公開前に実文が必須 |
| 同意項目の文言 | 同上。項目数と主旨のみ本SPECで確定させる（§8.3） |
| ロゴ・OGP画像 | 当面は既存の猫モチーフSVGを流用する |
| Webフォントの書体とウェイト | セルフホストは確定。何を何ウェイト積むかは実装時に決める。容量に効く |
| Chronos から Pawgress へのリネーム実施 | §9 |
| 有償版の値引き（4,980円から2,980円）の終了条件 | 旧SPEC では正式UATを経た動作保証版のリリース時点とし、期限は設けない |
| サブスクの認可・再取得の設計と同意項目 | 導入を決めた時点で別途起こす（§8.2） |
| Cloudflare / Stripe アカウントの運用（2FA・鍵のローテーション・支払い方法の期限） | 技術仕様ではないため対象外。再構築手順（§7.5）に情報だけ残す |
| AIサービスの利用料の扱い | 「利用者と各AI事業者のあいだの契約であり Pawgress の価格に含まれない」旨を `【仮】` で書いてある（§7.1 の setup 節）。表現と、無料枠の案内を添えるかどうかを要確認 |

### 10.2 実装前に確認すること

判断ではなく、調べれば決まるもの。実装タスクの最初に潰す。

| # | 確認すること | 現時点で分かっていること |
|---|---|---|
| 1 | R2 バケット内の実際のオブジェクトキー | コード上は `latest/Chronos-Setup.exe` と `latest/Chronos-Free-Setup.exe` |
| 2 | CSP を入れた状態で全ページが動くか | 実機確認が必要。許可先はアナリティクス1つで済む見込み |
| 3 | 存在しないURLが実際に 404 ステータスを返すか | Workers Static Assets の挙動を確認する |
| 4 | `observability` 未記述でデプロイしたとき Workers Logs が無効化されるか | 新規 Worker は既定で有効。書かない場合の挙動はドキュメント上あいまいで、1行書けば確実 |
| 5 | Stripe ダッシュボードのイベント／ログの保持期間 | 30日程度 |
| 6 | カスタム要素の展開が初回描画前に入るか（実機） | 理屈上は入る |
| 7 | `request.cf.country` が期待どおり取れるか（ローカル `wrangler dev` を含む） | Cloudflare が付与する標準フィールド。ローカル開発時の挙動だけ要確認 |
| 8 | 閾値ゼロ地域（EU・UK・韓国・インド・メキシコ）の一覧が正しいか | 本書の整理は2026年9月時点。金額が動く前に専門家に当てる |
| 8b | **他に閾値ゼロの地域が無いか。今後増えないか** | 一覧は網羅ではなく、非居住者には閾値が適用されない制度もある（§7.2）。専門家確認とセット |
| 8c | **無料版のダウンロードに地域制限が要るか**（`/api/download-free`） | 決済が無いため税務の論点は発生しないが、制裁対象国への無償配布と輸出規制、無償利用者に対する GDPR は別問題（§8.1）。結果しだいで判定を追加する |
| 9 | 英語ページに USD の参考表記を出してよいか | 出せば英語圏の転換率は上がるが、表示通貨とみなされると §7.2 の防御が弱まる。8 とセットで判断する |

### 10.3 実装時に直すもの

| # | 内容 |
|---|---|
| 1 | `wrangler.jsonc` の `name` が `chronos-lp-test`。workers.dev のサブドメインに露出するため、ドメイン確定時かリネーム実施時のいずれか早いほうで変える |
| 2 | `functions/api/checkout.js` が `STRIPE_PRICE_ID` を直接読み、`success_url` も `/thanks.html` を指している。レジストリ導入時にまとめて直す |
| 3 | `public/src/README.md` が `/src/README.md` として公開されている。移設時に `public/` の外へ出す |
| 4 | 現行の資産配置が一貫していない（CSS は `public/` 直下、画像は `images/`、JS だけ `src/`）。`public/assets/` への統一で解消する |
| 5 | 現行 `/` からの転送を `_redirects` に入れる（既存のブックマークと被リンク対策） |
| 6 | 現行 `public/docs/` の APIキー取得ガイドは、共通（AIサービス側の発行手順）と固有（Pawgress への設定手順）が1本に混ざっている。移設時に分割する |
| 7 | `README.md` の中身が `"# Test"` の1行のみ。整備する |
